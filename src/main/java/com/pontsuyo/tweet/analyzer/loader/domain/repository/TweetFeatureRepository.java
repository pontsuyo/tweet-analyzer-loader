package com.pontsuyo.tweet.analyzer.loader.domain.repository;

import com.pontsuyo.tweet.analyzer.loader.domain.model.TweetFeature;

public interface TweetFeatureRepository {
	void updateTweetFeature(TweetFeature tweetFeature);
}
